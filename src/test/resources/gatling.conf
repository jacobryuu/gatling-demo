gatling {
  core {
    runDescription = "gatling-demo performance run"
    encoding       = "utf-8"
  }
  http {
    ahc {
      connectTimeout       = 10000
      requestTimeout       = 60000
    }
  }
}
